[alias]
    alias = !git config --list | grep 'alias\\.' | sed 's/alias\\.\\([^=]*\\)=\\(.*\\)/\\1\\\t => \\2/'
    info  = remote show origin
    a   = add -p . #with TermReadKey
    b   = branch
    d   = diff
    f   = fetch --prune
    g   = grep -nH --break --color --heading
    l   = log --date=iso --graph --pretty=format:'%Cgreen%cd %Cblue%cn %Cred%h\n  %Creset%s'
    s   = status --branch --short
    aa  = add .
    ba  = branch --all
    cm  = commit --message
    co  = checkout
    lv  = log --date=iso --graph --name-status --pretty=format:'%Cgreen%cd %Cblue%cn %Cred%h\n  %Creset%s'

[color]
    ui      = true

[core]
    editor  = vim
    pager   = less -r
    excludesfile = ~/.gitignore-rkmathi

[interactive]
    singlekey = true

[merge]
    ff = false

[push]
    default = tracking

[user]
    name    = Ryuichi KAWAMATA (rkmathi)
    email   = i@rkmathi.net

